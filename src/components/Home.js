import React from 'react';
import { Records } from "./Records";

// Componente para a tela principal
export const Home = () => {
  return (
    <div className="mt-4">
      <Records />
    </div>
  );
}

export default Home;
